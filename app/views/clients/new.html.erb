<h1>New Client</h1>

<%= render 'form' %>

<%= form_for @client, url: clients_path, html: { multipart: true } do |form| %>
  <%= form.file_field :before_image_front %>
  <%= form.file_field :before_image_side %>
<% end %>

<%= link_to 'Back', clients_path %>
